class Solution {
    public int thirdLargest(List<Integer> arr) {
        if(arr.size()<3)
        {
            return -1;
        }
       arr.sort(null);
       return arr.get(arr.size()-3);
    }
}
