class Solution {
    public int findElementAtIndex(int i, int[] arr) {
        // code here
        int found=arr[i];
        for(i=0;i<arr.length;i++)
        {
            if(found==arr[i])
            {
                return arr[i]; 
            }
        }
        return -1;
    }
}
